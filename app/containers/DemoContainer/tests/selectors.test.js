// import { fromJS } from 'immutable';
// import { makeSelectDemoContainerDomain } from '../selectors';

// const selector = makeSelectDemoContainerDomain();

describe('makeSelectDemoContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
