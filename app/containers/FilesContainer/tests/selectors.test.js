// import { fromJS } from 'immutable';
// import { makeSelectFilesContainerDomain } from '../selectors';

// const selector = makeSelectFilesContainerDomain();

describe('makeSelectFilesContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
