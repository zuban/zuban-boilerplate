// import { fromJS } from 'immutable';
// import { makeSelectUploadContainerDomain } from '../selectors';

// const selector = makeSelectUploadContainerDomain();

describe('makeSelectUploadContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
