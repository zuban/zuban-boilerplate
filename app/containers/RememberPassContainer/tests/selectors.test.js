// import { fromJS } from 'immutable';
// import { makeSelectRememberPassContainerDomain } from '../selectors';

// const selector = makeSelectRememberPassContainerDomain();

describe('makeSelectRememberPassContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
