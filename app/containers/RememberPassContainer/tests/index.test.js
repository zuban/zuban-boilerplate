// import React from 'react';
// import { shallow } from 'enzyme';

// import { RememberPassContainer } from '../index';

describe('<RememberPassContainer />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
