// import { fromJS } from 'immutable';
// import { makeSelectLoginContainerDomain } from '../selectors';

// const selector = makeSelectLoginContainerDomain();

describe('makeSelectLoginContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
