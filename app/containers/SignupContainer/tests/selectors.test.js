// import { fromJS } from 'immutable';
// import { makeSelectSignupContainerDomain } from '../selectors';

// const selector = makeSelectSignupContainerDomain();

describe('makeSelectSignupContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
